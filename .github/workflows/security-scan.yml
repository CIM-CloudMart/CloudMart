name: Security & Vulnerability Scan

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

permissions:
  contents: read
  security-events: write

jobs:
  trivy-scan:
    name: Trivy Repository Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy filesystem scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'          # report only — don't block until vulns are triaged

      - name: Upload Trivy SARIF to GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Generate SBOM
        if: always()
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'cyclonedx'
          output: 'sbom.json'

      - name: Upload SBOM artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ github.sha }}
          path: sbom.json

  secret-scan:
    name: Secret Scan (TruffleHog)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run TruffleHog (full repo scan, no base/head diff)
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          extra_args: --only-verified

  dependency-audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install pip-audit
        run: pip install pip-audit --quiet

      # Node services — generate lockfile if missing, then audit
      - name: Audit frontend
        working-directory: services/frontend
        run: |
          if [ ! -f package-lock.json ]; then
            npm install --package-lock-only --ignore-scripts
          fi
          npm audit --audit-level=critical
        continue-on-error: true   # report only until lockfiles are committed

      - name: Audit order-service
        working-directory: services/order-service
        run: |
          if [ ! -f package-lock.json ]; then
            npm install --package-lock-only --ignore-scripts
          fi
          npm audit --audit-level=critical
        continue-on-error: true

      - name: Audit notification-service
        working-directory: services/notification-service
        run: |
          if [ ! -f package-lock.json ]; then
            npm install --package-lock-only --ignore-scripts
          fi
          npm audit --audit-level=critical
        continue-on-error: true

      # Python services
      - name: Audit product-service
        working-directory: services/product-service
        run: pip-audit -r requirements.txt
        continue-on-error: true

      - name: Audit user-service
        working-directory: services/user-service
        run: pip-audit -r requirements.txt
        continue-on-error: true

  scan-summary:
    name: Scan Summary
    runs-on: ubuntu-latest
    needs: [trivy-scan, secret-scan, dependency-audit]
    if: always()
    steps:
      - name: Check all scan results
        run: |
          echo "Trivy:   ${{ needs.trivy-scan.result }}"
          echo "Secrets: ${{ needs.secret-scan.result }}"
          echo "Deps:    ${{ needs.dependency-audit.result }}"
          if [[ "${{ needs.secret-scan.result }}" == "failure" ]]; then
            echo "Secret scan failed — verified secrets found in repo. Blocking."
            exit 1
          fi
          echo "All scans completed."