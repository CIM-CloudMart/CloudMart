name: CD Staging (Temporary)

on:
  workflow_dispatch:

jobs:
  deploy_staging:
    if: github.ref == 'refs/heads/feat/workflow-container-deployment'
    runs-on: ubuntu-latest
    steps:
      - name: Temporary placeholder
        run: echo "Temporary manual-only workflow for feat/workflow-container-deployment"
